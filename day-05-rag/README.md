# Day 5: Basic RAG & Binary Search 📚🔍

## 🤖 1. Basic RAG Implementation (`basic_rag.py`)
Built a foundational Retrieval-Augmented Generation script. The code reads a local text file (`company_policy.txt`) using `open()`, constructs an augmented prompt using `f-strings`, and instructs the Groq LLM to answer user queries strictly based on the provided document context.

## ⚡ 2. DSA Optimization (`binary_search.py`)
Solved LeetCode 704 (Binary Search). Implemented an $O(\log n)$ search algorithm utilizing `left` and `right` pointers and the `//` floor division operator to efficiently locate a target within a sorted array.